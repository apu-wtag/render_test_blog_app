<!DOCTYPE html>
<html>
<head>
  <title>Admin - Blog App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-white">
<%= render "admin/shared/nav" %>

<main>
  <% if flash[:notice] %>
    <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mx-auto max-w-7xl sm:px-6 lg:px-8 mt-4" role="alert">
      <p><%= notice %></p>
    </div>
  <% end %>
  <% if flash[:alert] %>
    <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mx-auto max-w-7xl sm:px-6 lg:px-8 mt-4" role="alert">
      <p><%= alert %></p>
    </div>
  <% end %>

  <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
    <%= yield %>
  </div>
</main>
</body>
</html>